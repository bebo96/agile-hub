<div class="tasks">
  {{#each this}}
  <section class="task">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{task_text}}
    </div>
  </section>
  {{/each}}
</div>