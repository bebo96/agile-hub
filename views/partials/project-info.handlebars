<article class="project card text-center bg-dark btn-no-style">
  <div class="title list-group list-group-flush btn-no-style">
    <a class="btn-no-style" href="/project/{{id}}"><p class="project-title">{{title}}</p></a>
    <span><p class="customer-name">{{customer_name}}</p></span>
  </div>
  <div id="projectNumber"class="meta list-group-item bg-warning">
    <a href="/project/{{id}}" class="btn btn-primary ">{{tasks.length}} tasks</a>
  </div>
</article>