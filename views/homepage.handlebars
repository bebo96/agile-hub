{{!-- <ol class="task-list">
  <li>
    <article class="task">
      <div class="title">
        <a href="{{task_url}}" target="_blank">{{title}}</a>
        <span>({{task_url}})</span>
      </div>
      <div class="meta">
        {{vote_count}} point(s) by {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div>
    </article>
  </li>
</ol> --}}